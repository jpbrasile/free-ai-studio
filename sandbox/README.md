# Sandbox resources

La logique d'exécution se trouve dans `sandbox-manager/` et `sandbox-worker/`.

- `sandbox-manager`: orchestration Local/Kaggle/Colab + registre d'artefacts.
- `sandbox-worker`: exécution Python locale isolée et sans Internet.
- Documentation: `../docs/SANDBOX.md`.
