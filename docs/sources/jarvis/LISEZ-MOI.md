# Plan JARVIS — source du point 16 de PLAN.md

Ces fichiers ne décrivent **pas** le Studio. Ils sont le plan d'un autre projet, JARVIS :
un assistant vocal qui pilote NotebookLM, avec un tuteur, un tableau vivant et un mode
confidentiel 100 % local. Le propriétaire les a déposés le 23/09/2026 pour qu'on en tire
ce qui s'applique au Studio ; ce qui en a été tiré est au **point 16 de `PLAN.md`**.

Gardés tels quels, sans modification, pour que chaque idée du point 16 puisse être
relue à sa source :

- `jarvis-plan/` : le contenu de `jarvis-plan.zip` (plan, phases, risques, règles).
- `PLAN-version-anterieure.md` : `PLAN (1).md`, une version **plus ancienne** du même
  `PLAN.md`, sans l'étape C « confidentiel ».
- `13-mode-confidentiel.md` a aussi été déposé à part ; il est identique à
  `jarvis-plan/phases/13-mode-confidentiel.md` et n'est donc pas copié deux fois.

Aucun code du Studio ne lit ces fichiers. Les noms de modèles qu'ils citent n'ont pas
été vérifiés par le Studio.
