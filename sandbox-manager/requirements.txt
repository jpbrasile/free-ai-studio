fastapi==0.116.1
uvicorn[standard]==0.35.0
httpx==0.28.1
# Meme version que free-tier-manager : la CI installe les trois fichiers dans
# UN SEUL environnement, et deux epingles differentes du meme paquet y rendent
# l'installation impossible. Les images, elles, installent chacune la sienne et
# ne voyaient donc rien -- 36 passages rouges d'affilee, du 15/09 au 20/09/2026.
python-multipart==0.0.32
kaggle==2.2.4
modal==1.5.5
huggingface_hub==1.32.0
# Le coffre des secrets : les magasins gardent les noms en clair et les valeurs
# non (`coffre.py`). MEME epingle que l'autre service -- la CI installe les trois
# fichiers dans un seul environnement. Mesure le 22/09/2026 : absent des DEUX
# images avant ce jour, donc c'est bien une dependance neuve et non un transitif.
cryptography==46.0.4
# Le lecteur de PDF de la brique `document_lecture` (`document.py`). BSD-3,
# pur Python, AUCUNE dependance a l'execution sur Python >= 3.11 -- verifie
# sur PyPI le 22/09/2026, et l'image est `python:3.12-slim`. Absent des deux
# images avant ce jour : c'est une dependance neuve, pas un transitif. Il
# n'est PAS ajoute a `free-tier-manager` : ce service ne lit aucun document.
pypdf==6.19.0
# NotebookLM (« Gemini Notebook ») par ses accès NON DOCUMENTÉS (`notebooklm_pont.py`,
# PLAN.md 17.6). MIT. Épinglé : une API non documentée casse sans prévenir, et
# la version se change exprès, après essai. Dépendances à l'exécution : httpx,
# click, rich, filelock (relu sur PyPI le 24/09/2026) ; Playwright n'est PAS
# installé -- il ne sert qu'à `notebooklm login`, fait hors du Studio.
notebooklm-py==0.8.2
